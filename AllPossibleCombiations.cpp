#include <iostream>
#include <stack>
#include <set>
#include <vector>

using namespace std;

void checkAllCombinations(std::vector<int> A, int sum, std::pri std::set<std::vector<int>> ret)
{
    if (sum == 0)
    {
        
        
        return;
    }
}

public:
vector<vector<int>> combinationSum(vector<int> &A, int N, int B)
{
    set<vector<int>> s;
    
    checkAllCombinations(A, B, s);
    
    vector<vector<int>> ret;
    
    
    
    return ret;
}