#include <iostream>

auto getAllCombinations(std::string& str)
{
    int n = pow(2, str.size());

    for (int i = 0; i < n; i++)
    {
        
    }
}

int main()
{
    std::string str;

    auto vect = getAllCombinations(str);
}